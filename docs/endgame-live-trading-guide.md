# 🚀 Endgame Live Trading 使用指南

## 📋 目录

- [概述](#概述)
- [新增功能](#新增功能)
- [快速开始](#快速开始)
- [配置说明](#配置说明)
- [Dry Run 测试](#dry-run-测试)
- [小额实盘](#小额实盘)
- [常见问题](#常见问题)

---

## 概述

**Endgame Live Trading** 是基于 1300+ 笔模拟交易验证的尾盘策略实盘版本，集成了完整的安全保护机制。

### 策略特点

- **验证数据**: v2/v3/v4 三个版本，1300+ 笔交易
- **稳定策略**:
  - 2m + LOW (0.75-0.84) + SL15% → ROI: 35%→57%→123%
  - 2m + MID_LOW (0.85-0.89) + SL15% → ROI: 47%→60%→40%
- **进场时机**: 剩余 < 2 分钟
- **止盈**: bid >= 0.97
- **止损**: 15%

### 安全机制

✅ Kill Switch - 紧急停止
✅ Fund Limiter - 资金上限
✅ Loss Circuit Breaker - 亏损熔断
✅ 环境隔离 - Dry Run / Live 模式

---

## 新增功能

### 相比你的原始脚本

| 功能 | 原始脚本 | 新版本 |
|-----|---------|--------|
| **真实下单** | ❌ TODO | ✅ 完整实现 |
| **订单管理** | ❌ | ✅ 追踪 Order ID |
| **Token ID 获取** | ❌ | ✅ 自动获取 |
| **市价单执行** | ❌ | ✅ FOK 订单 |
| **Kill Switch** | ❌ | ✅ 集成 |
| **资金上限** | ❌ | ✅ 多层限制 |
| **亏损熔断** | ❌ | ✅ 自动触发 |
| **环境配置** | 硬编码 | ✅ .env 文件 |
| **错误处理** | 基础 | ✅ 完善 |

### 核心改进

1. **真实下单实现**
   ```typescript
   // 开仓: 市价买单 (FOK)
   await sdk.tradingService.createMarketOrder({
     tokenId,
     side: 'BUY',
     amount: cost,
     price: entryPrice * 1.02, // +2% 滑点保护
     orderType: 'FOK',
   });

   // 平仓: 市价卖单 (FOK)
   await sdk.tradingService.createMarketOrder({
     tokenId,
     side: 'SELL',
     amount: shares,
     price: exitPrice * 0.98, // -2% 滑点保护
     orderType: 'FOK',
   });
   ```

2. **Token ID 管理**
   - 自动从市场数据获取 YES/NO token IDs
   - 缓存 token IDs 避免重复查询

3. **Trading Guard 集成**
   - 每笔交易前检查所有保护机制
   - 自动记录开仓/平仓到 Fund Limiter 和 Loss Breaker

---

## 快速开始

### 前置条件

```bash
# 1. 确保在项目根目录
cd /home/user/poly-sdk

# 2. 安装依赖
npm install

# 3. 构建项目（如果还没有）
npm run build
```

### Dry Run 测试（推荐）

```bash
# 1. 复制配置
cp .env.endgame-live .env

# 2. 确认是 Dry Run 模式
cat .env | grep DRY_RUN
# 应该显示: DRY_RUN=true

# 3. 启动测试
npx tsx scripts/endgame/live-trading.ts
```

### 观察输出

脚本会显示：
- 🔸 **DRY RUN (模擬)** - 顶部黄色标识
- 市场扫描和价格监控
- 模拟开仓/平仓信号
- 实时 P&L 统计
- Trading Guard 状态

---

## 配置说明

### 环境变量

```bash
# .env.endgame-live

# ─────────────────────────────────────
# 交易模式
# ─────────────────────────────────────
DRY_RUN=true              # true = 模擬, false = 真實

# ─────────────────────────────────────
# 私鑰 (Live 必需)
# ─────────────────────────────────────
PRIVATE_KEY=0x...         # 你的錢包私鑰

# ─────────────────────────────────────
# Kill Switch
# ─────────────────────────────────────
KILL_SWITCH_FILE=/tmp/poly-kill-switch-endgame

# ─────────────────────────────────────
# Fund Limiter
# ─────────────────────────────────────
MAX_DAILY_VOLUME=100      # 每日最大交易額
MAX_TOTAL_POSITION=200    # 最大總持倉
MAX_SINGLE_TRADE=50       # 單筆最大金額

# ─────────────────────────────────────
# Loss Circuit Breaker
# ─────────────────────────────────────
MAX_DAILY_LOSS=30         # 單日最大虧損
MAX_TOTAL_LOSS=50         # 累計最大虧損
MAX_CONSECUTIVE_LOSSES=5  # 最大連續虧損次數

# ─────────────────────────────────────
# 策略參數
# ─────────────────────────────────────
INITIAL_CAPITAL=100       # 初始資金
CAPITAL_PERCENT=0.5       # 每筆 50%
MAX_CONCURRENT=2          # 最多 2 持倉
MIN_ORDER_SIZE=5          # 最小 $5
MAX_ORDER_SIZE=50         # 最大 $50
```

### 策略配置（脚本内部）

```typescript
// 不建议修改，除非你非常了解策略逻辑
const STRATEGY_CONFIG = {
  maxMinutesLeft: 2,        // 剩餘 < 2 分鐘進場
  minMinutesLeft: 0.1,      // 最少 6 秒

  priceRanges: {
    LOW: { min: 0.75, max: 0.84 },
    MID_LOW: { min: 0.85, max: 0.89 },
  },

  takeProfitPrice: 0.97,    // 止盈
  stopLossPercent: 0.15,    // 止損 15%
};
```

---

## Dry Run 测试

### 测试流程

```bash
# 1. 设置 Dry Run 环境
export DRY_RUN=true

# 2. 启动测试
npx tsx scripts/endgame/live-trading.ts

# 3. 观察运行（建议 24+ 小时）
# - 是否正常扫描市场
# - 是否在正确时机进场
# - 是否正确执行止盈/止损
# - Trading Guard 是否正常工作

# 4. 测试 Kill Switch
# 在另一个终端:
touch /tmp/poly-kill-switch-endgame
# 观察脚本应该立即停止

# 5. 重置 Kill Switch
rm /tmp/poly-kill-switch-endgame

# 6. 检查日志
ls -lh logs/endgame-dry-*.csv
cat logs/endgame-dry-*.csv
```

### 验证清单

- [ ] 运行 24+ 小时无错误
- [ ] 至少执行 10+ 笔模拟交易
- [ ] 策略表现符合预期 (ROI > 0%)
- [ ] Kill Switch 测试通过
- [ ] Fund Limiter 正确触发
- [ ] Loss Breaker 正确触发
- [ ] 所有保护机制生效

---

## 小额实盘

### ⚠️ 开始前必读

1. **Dry Run 测试通过** - 至少 24 小时
2. **理解所有风险** - 可能损失本金
3. **小额起步** - $50-100
4. **准备应急预案** - Kill Switch 使用方法
5. **实时监控** - 不要让脚本无人值守

### 配置实盘

```bash
# 1. 编辑配置
nano .env.endgame-live

# 修改以下内容:
# DRY_RUN=false
# PRIVATE_KEY=0x你的私钥

# 2. 小额配置（推荐）
# INITIAL_CAPITAL=100
# MAX_DAILY_VOLUME=100
# MAX_SINGLE_TRADE=50
# MAX_DAILY_LOSS=30
# MAX_TOTAL_LOSS=50
```

### 启动实盘

```bash
# 1. 加载配置
export $(cat .env.endgame-live | xargs)

# 2. 确认配置
echo "DRY_RUN: $DRY_RUN"
echo "INITIAL_CAPITAL: $INITIAL_CAPITAL"
echo "KILL_SWITCH: $KILL_SWITCH_FILE"

# 3. 启动（会有 10 秒确认期）
npx tsx scripts/endgame/live-trading.ts

# 脚本会显示:
# ⚠️  LIVE TRADING - 真實資金將被使用
# Press Ctrl+C to cancel, or wait 10 seconds to continue...
#
# 如果确认无误，等待 10 秒后自动开始
```

### 实时监控

```bash
# 方法 1: 在另一个终端监控日志
tail -f logs/endgame-live-*.csv

# 方法 2: 监控保护机制状态
watch -n 5 "cat /tmp/poly-fund-limiter.json | jq '.'"

# 方法 3: 监控 Kill Switch
watch -n 1 "ls -lh /tmp/poly-kill-switch-* 2>/dev/null || echo 'OK'"
```

### 紧急停止

```bash
# 最快方式: Kill Switch
touch /tmp/poly-kill-switch-endgame

# 备用方式: Ctrl+C
# 在运行脚本的终端按 Ctrl+C

# 远程停止（如果 SSH 断开）
ssh your-server "touch /tmp/poly-kill-switch-endgame"
```

---

## 常见问题

### Q1: 为什么没有进场？

**可能原因**:
1. **市场价格不在目标区间**
   - 查看 dashboard 的市场价格
   - 确认是否有 `[LOW]` 或 `[MID_LOW]` 标记

2. **时机不对**
   - 策略只在剩余 < 2 分钟时进场
   - 查看市场的剩余时间

3. **已达持仓上限**
   - 默认最多 2 个同时持仓
   - 等待现有持仓平仓

4. **资金不足**
   - 每笔至少需要 $5
   - 检查当前 capital

5. **Trading Guard 阻止**
   - 检查 Fund Limiter 是否达到限制
   - 检查 Loss Breaker 是否触发
   - 检查 Kill Switch 是否激活

### Q2: 订单失败怎么办？

**Live 模式订单失败原因**:
1. **余额不足**
   - 检查钱包 USDC 余额
   - 确保有足够的 gas fee (MATIC)

2. **网络问题**
   - 检查网络连接
   - 稍后重试

3. **市场流动性不足**
   - 订单簿深度不够
   - 策略会自动跳过

4. **滑点过大**
   - 价格变化超过 2%
   - 订单自动取消（FOK）

**解决方法**:
- 脚本会自动处理失败，不会扣除资金
- 失败的交易不会记录到统计
- 可以在日志中查看错误信息

### Q3: 如何调整策略参数？

**可调整参数**:

1. **资金管理** (.env 文件):
   ```bash
   INITIAL_CAPITAL=100      # 初始资金
   CAPITAL_PERCENT=0.5      # 每笔 50%
   MAX_CONCURRENT=2         # 最多 2 持仓
   MIN_ORDER_SIZE=5         # 最小 $5
   MAX_ORDER_SIZE=50        # 最大 $50
   ```

2. **风控参数** (.env 文件):
   ```bash
   MAX_DAILY_VOLUME=100     # 日交易额
   MAX_DAILY_LOSS=30        # 日亏损
   MAX_TOTAL_LOSS=50        # 累计亏损
   ```

3. **策略参数** (需修改脚本):
   ```typescript
   // 不建议修改，除非你完全理解
   maxMinutesLeft: 2,       // 进场时机
   takeProfitPrice: 0.97,   // 止盈
   stopLossPercent: 0.15,   // 止损
   ```

### Q4: Dry Run vs Live 的区别？

| 项目 | Dry Run | Live |
|-----|---------|------|
| 下单 | ❌ 模拟 | ✅ 真实 |
| 私钥 | 不需要 | 必需 |
| 资金 | 虚拟 | 真实 USDC |
| Order ID | 无 | 有 |
| 风险 | 零 | 真实资金风险 |
| 确认步骤 | 无 | 10 秒确认期 |

### Q5: 如何查看历史交易？

```bash
# 查看最新的 CSV 文件
ls -lht logs/ | head

# 查看交易详情
cat logs/endgame-live-2026-01-11T12-00-00.csv

# 统计分析
# 总交易数
wc -l logs/endgame-live-*.csv

# 盈利交易数
cat logs/endgame-live-*.csv | awk -F, '$11 > 0 {count++} END {print count}'

# 平均盈亏
cat logs/endgame-live-*.csv | awk -F, 'NR>1 {sum+=$11; count++} END {print sum/count}'
```

### Q6: 如何备份和恢复？

```bash
# 备份配置
cp .env.endgame-live .env.endgame-live.backup

# 备份日志
tar -czf endgame-logs-$(date +%Y%m%d).tar.gz logs/

# 备份保护机制状态
cp /tmp/poly-fund-limiter.json backups/
cp /tmp/poly-loss-breaker.json backups/
```

---

## 📊 性能预期

### 基于历史数据

**v2 测试 (600+ trades)**:
- LOW range: ROI +35.46%, Win Rate 68.2%
- MID_LOW range: ROI +47.26%, Win Rate 76.9%

**v3 测试 (400+ trades)**:
- LOW range: ROI +57.16%, Win Rate 65.6%
- MID_LOW range: ROI +60.73%, Win Rate 86.7%

**v4 测试 (300+ trades)**:
- LOW range: ROI +123.04%, Win Rate 72.3%
- MID_LOW range: ROI +40.48%, Win Rate 74.4%

### 小额实盘预期

**保守预期**:
- 日交易: 5-10 笔
- 胜率: 65-75%
- 日 ROI: +5% ~ +15%
- 最大回撤: < 10%

**注意**:
- 历史表现不代表未来
- 市场条件可能变化
- 实盘可能有滑点损失
- 建议先小额测试 1-2 天

---

## 🆘 获取帮助

### 文档

- **安装指南**: `SETUP.md`
- **实盘准备**: `docs/live-trading-readiness.md`
- **技术总结**: `SAFETY-ENHANCEMENTS-SUMMARY.md`

### 问题排查

1. 查看日志文件
2. 检查保护机制状态
3. 查看网络连接
4. 确认余额充足

### 反馈

- GitHub Issues
- Pull Requests
- 社区讨论

---

## ⚠️ 风险提示

1. **交易有风险** - 可能损失全部本金
2. **策略可能失效** - 市场条件变化
3. **技术风险** - 网络、API 故障
4. **保护机制不是 100%** - 无法保证无风险

**建议**:
- 小额起步 ($50-100)
- 严格风控 (设置合理限额)
- 实时监控 (不要无人值守)
- 快速响应 (异常立即停止)

---

**祝交易顺利！** 🚀
